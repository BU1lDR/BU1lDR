<!-- A PROFILE.md that leans on every placeholder. {live} {version} {license} -->
# Curated $1 -- {version} on {license} $1 \1 ([live]({live}))

{license} · {version} · {name} · {url}

{description}

Second paragraph with `code`, **bold**, `{version}` in a code span (renders with the escapes, as documented), {{version}} as a literal, and a link to [a sibling](https://github.com/hostile-owner/fallback).

<!-- Copy this file to .github/PROFILE.md in a repository and fill it in. The profile
     README at github.com/BU1lDR renders it within the hour (tools/build_readme.py in
     BU1lDR/BU1lDR). Leading HTML comments like this one are ignored.

     Shape -- keep it: one heading line, one meta row, at most two short paragraphs.
     The profile is a summary; the detail belongs in this repository's README, where
     its CI can hold it to the code. A blurb over the configured character limit still
     renders but the run warns.

     Placeholders the builder fills from the GitHub API:
       {license}      SPDX id GitHub detected on the repository
       {version}      newest published release tag ("unreleased" if none -- omit it
                      from the meta row until the first release)
       {live}         the repository's homepage URL (error if the repository has none)
       {description}  the repository description
       {name} {url}   the repository's name and URL
     Write {{version}} for a literal "{version}". Do not put a test count or any other
     figure another file already owns here unless this repository's CI checks it. -->

# Display name — what it is, in one clause

{license} · {version} · language · main libraries

What it does, for whom, and the one design decision worth knowing. Two or three sentences.

What is verified rather than claimed: the check that would catch the README lying, in one or two sentences.
